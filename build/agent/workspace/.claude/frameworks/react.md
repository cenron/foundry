# React Conventions

- Vite + TypeScript + Tailwind CSS + shadcn/ui
- TanStack Query for server state — no useEffect + useState for data fetching
- Feature-folder structure: `src/features/<name>/`
- Playwright for E2E tests
